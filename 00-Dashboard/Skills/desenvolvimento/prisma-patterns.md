# prisma-patterns
Categoria: desenvolvimento

Padrões Prisma (ORM TypeScript): schema, transações, paginação e armadilhas (updateMany retorna contagem, $transaction timeout, migrate dev reseta DB, connectio...

Fonte: `10-Inteligencia/skills/desenvolvimento/prisma-patterns/SKILL.md`
